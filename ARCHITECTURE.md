# OpenFlexure显微镜项目架构文档

## 项目简介

OpenFlexure显微镜是一个开源的3D打印显微镜项目，旨在提供低成本、高精度的科学显微镜解决方案。该项目采用模块化设计，支持从基础网络摄像头到专业显微镜物镜的多种光学配置。

## 核心技术特点

### 1. 柔性机构技术
- **无摩擦运动**: 通过塑料的弹性变形实现精密运动，避免传统机械滑动产生的摩擦和间隙
- **亚微米精度**: 在约10mm的运动范围内实现100nm以下的步进精度
- **高稳定性**: 数天内位置稳定性在几微米范围内

### 2. 参数化3D设计
- **OpenSCAD建模**: 使用代码驱动的3D建模，便于参数化定制
- **模块化架构**: 各组件独立设计，通过标准接口连接
- **可配置性**: 通过修改参数文件快速生成不同规格的显微镜

### 3. 集成电子控制
- **树莓派平台**: 提供完整的Linux环境和网络连接
- **电机驱动**: 支持步进电机实现三轴自动控制
- **Web界面**: 通过浏览器进行远程控制和图像查看

## 系统架构

### 机械系统架构

```
显微镜机械系统
├── 主体结构 (main_body.scad)
│   ├── XY载物台
│   │   ├── 柔性支腿 (4-bar linkage)
│   │   ├── XY致动器 (2个)
│   │   └── 样品夹具接口
│   └── 机身底座
│       ├── 电机安装座
│       ├── 照明系统接口
│       └── 电子设备安装位
├── Z轴系统 (z_axis.scad)
│   ├── 物镜安装座
│   ├── Z轴致动器
│   ├── 光学模块接口
│   └── 对焦机制
├── 光学系统 (optics.scad)
│   ├── 相机模块接口
│   ├── 镜头安装系统
│   ├── 管镜系统（可选）
│   └── 滤光片接口（可选）
├── 照明系统 (illumination.scad)
│   ├── LED安装座
│   ├── 聚光镜系统
│   ├── 透射照明臂
│   └── 反射照明模块（可选）
└── 支撑系统
    ├── 前支脚 (feet.scad) x3
    ├── 后支脚 (back_foot.scad)
    └── 底座支架（可选）
```

### 致动器系统设计

每个致动器包含以下组件：
- **螺杆机制**: M3螺杆提供线性运动
- **柔性连接**: 将螺杆运动传递到载物台
- **螺母固定**: 特殊设计的螺母座
- **弹性预紧**: 橡胶带提供预紧力
- **电机接口**: 可选的步进电机连接

### 软件系统架构

```
软件系统
├── 嵌入式控制 (树莓派)
│   ├── OpenFlexure Server
│   │   ├── 设备控制API
│   │   ├── 图像采集
│   │   ├── 运动控制
│   │   └── Web服务器
│   ├── 相机驱动
│   ├── 电机控制驱动
│   └── 系统服务
├── 客户端软件
│   ├── OpenFlexure Connect
│   │   ├── 设备发现
│   │   ├── 用户界面
│   │   └── 图像显示
│   ├── Python客户端
│   │   ├── 脚本控制API
│   │   ├── 自动化功能
│   │   └── 数据采集
│   └── Web界面
│       ├── 实时控制
│       ├── 参数设置
│       └── 图像预览
└── 开发工具
    ├── 设备仿真器
    ├── 校准工具
    └── 测试套件
```

## 构建系统详解

### 构建流程
1. **参数解析**: 从`microscope_parameters.scad`读取配置
2. **依赖分析**: 分析OpenSCAD文件间的依赖关系
3. **任务生成**: 使用Ninja构建系统生成构建任务
4. **并行编译**: 多进程并行生成STL文件
5. **文件组织**: 按配置分类输出到builds目录

### 配置系统
项目支持多种预设配置，每种配置定义了：
- **光学系统**: 相机和镜头类型
- **机械配置**: 是否包含电机、支架类型
- **尺寸规格**: 载物台大小、工作距离
- **附加功能**: 照明、滤光片、夹具等

### STL文件生成规则
- **条件编译**: 根据配置参数决定是否生成特定文件
- **参数替换**: 在编译时注入配置参数
- **文件命名**: 根据配置自动生成描述性文件名
- **依赖管理**: 自动处理文件间的依赖关系

## 关键设计文件说明

### 核心设计文件
- **`microscope_parameters.scad`**: 全局参数定义，所有设计的基础
- **`main_body.scad`**: 显微镜主体，包含XY载物台和支撑结构
- **`compact_nut_seat.scad`**: 致动器设计，项目的核心机械组件
- **`utilities.scad`**: 通用函数库，提供常用的几何和数学函数

### 光学系统文件
- **`optics.scad`**: 主光学模块，支持多种光学配置
- **`cameras/`**: 各种相机模块的适配器设计
- **`lens_spacer.scad`**: 镜头间距调节器
- **`fl_cube.scad`**: 荧光滤光片立方体（可选）

### 运动系统文件
- **`z_axis.scad`**: Z轴系统，包含对焦机制
- **`gears.scad`**: 传动齿轮系统
- **`actuator_assembly_tools.scad`**: 组装工具设计

### 照明系统文件
- **`illumination.scad`**: 主照明系统
- **`reflection_illuminator.scad`**: 反射照明模块
- **`condenser.scad`**: 聚光镜设计

## 电子系统集成

### 硬件组件
1. **主控制器**: 树莓派4B（推荐）
2. **相机模块**: 
   - 树莓派相机模块v2（标准）
   - USB网络摄像头（低成本）
   - 工业相机（高端应用）
3. **电机驱动**: 
   - Sangaboard v0.3（推荐）
   - Arduino + 驱动板组合
4. **步进电机**: NEMA11或类似规格
5. **照明**: 高亮LED + 驱动电路

### 通信协议
- **REST API**: 基于HTTP的设备控制接口
- **WebSocket**: 实时数据传输
- **MQTT**: 可选的物联网协议支持

## 定制和扩展

### 参数定制
常用的定制参数包括：
```scad
// 尺寸参数
sample_z = 65;          // 载物台高度
leg_r = 30;             // 载物台半径
hole_r = 20;            // 中心孔径

// 功能选项
motor_lugs = true;      // 电机安装座
beamsplitter = false;   // 分光镜支持
endstops = false;       // 限位开关

// 光学配置
camera = "picamera_2";  // 相机类型
optics = "rms_f50d13";  // 光学系统
```

### 扩展接口
项目设计了多个标准化接口：
- **Dovetail接口**: 光学模块快速安装
- **螺纹接口**: 标准RMS物镜支持
- **电气接口**: 标准化的电机和传感器连接

### 社区贡献
- **硬件设计**: 新的相机适配器、附件设计
- **软件功能**: 图像处理、自动化脚本
- **文档改进**: 组装指南、故障排除
- **测试反馈**: 不同打印机和材料的测试结果

## 质量保证

### 设计验证
- **有限元分析**: 关键结构的应力分析
- **运动仿真**: 机械运动的精度验证
- **光学仿真**: 光路设计的理论验证

### 测试标准
- **精度测试**: 使用USAF测试板验证分辨率
- **重复性测试**: 长期稳定性验证
- **环境测试**: 不同温湿度条件下的性能

### 文档完整性
- **组装指南**: 详细的逐步组装说明
- **故障排除**: 常见问题和解决方案
- **维护指南**: 日常维护和校准程序

这个架构文档为理解和参与OpenFlexure显微镜项目提供了全面的技术指导。项目的开源特性使得它可以根据具体需求进行定制和改进。
